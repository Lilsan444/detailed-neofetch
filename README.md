# Detailed Neofetch

A more detailed version of `neofetch` that provides additional system information on Linux.

## Installation

You can install the package directly from the GitHub repository:

```bash
pip install git+https://github.com/Lilsan444/detailed-neofetch.git

